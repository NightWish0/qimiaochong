<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qimiaochong.common.dao.SysPermissionDao">

    <select id="getPermissionCodes" parameterType="int" resultType="string">
        SELECT p.permission_code FROM sys_permission p
        LEFT JOIN sys_role_permission rp ON p.id=rp.permission_id
        LEFT JOIN sys_user_role ur ON ur.role_id=rp.role_id
        WHERE ur.user_id=#{userId}
    </select>
</mapper>